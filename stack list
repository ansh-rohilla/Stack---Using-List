#include<iostream>
#include<vector>
#include<list>
using namespace std;
class Stack {
    list<int> ll;
    public:
    void push(int val){  // 0(1)
        ll.push_front(val);
    }
    void pop(){
        ll.pop_front(); // 0(1)
    }
    int top(){
        return ll.front(); // 0(1)
    }
    bool empty(){
        return ll.size() == 0; // 0(1)
    }
};
int main(){
    Stack s;
    s.push(10);
    s.push(20);
    s.push(30);
    while(!s.empty()){
        cout<<s.top()<<endl;
        s.pop(); // 0(1)
    }
    return 0;
}
